# Database — SQLite file path relative to src/backend/
DATABASE_URL="file:./prisma/dev.db"

# JWT — change this to a strong random string in production
JWT_SECRET="your-secret-key-change-in-production"

# Server
PORT=3001
